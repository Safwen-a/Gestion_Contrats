{% extends 'base.html.twig' %}

{% block title %}Categorie index{% endblock %}

{% block body %}
 <div style=" text-align: center; margin: 0px auto; display; block; "><h1>liste de Catégories des Clients</h1></div>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom_categorie</th>
                <th>Type</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for categorie in categories %}
            <tr>
                <td>{{ categorie.id }}</td>
                <td>{{ categorie.NomCategorie }}</td>
                <td>{{ categorie.type ? categorie.type|join(', ') : '' }}</td>
                <td>
                    <a href="{{ path('app_categorie_show', {'id': categorie.id}) }}">show</a>
                    <a href="{{ path('app_categorie_edit', {'id': categorie.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_categorie_new') }}">Create new</a>
{% endblock %}
