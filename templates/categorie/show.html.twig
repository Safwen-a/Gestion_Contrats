{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
   <div style=" text-align: center; margin: 0px auto; display; block; "> <h1>Categorie</h1></div>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Nom_categorie</th>
                <td>{{ categorie.NomCategorie }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ categorie.type ? categorie.type|join(', ') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" role="button" href="{{ path('app_categorie_index') }}">back to list</a>

    <a class="btn btn-primary" role="button" href="{{ path('app_categorie_edit', {'id': categorie.id}) }}">edit</a>

    {{ include('categorie/_delete_form.html.twig') }}
{% endblock %}
