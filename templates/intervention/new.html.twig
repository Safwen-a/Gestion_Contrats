{% extends 'base.html.twig' %}

{% block title %}New FicheIntervention{% endblock %}

{% block body %}
    <h1>Create new FicheIntervention</h1>

    {{ include('intervention/_form.html.twig') }}

    <a class="btn btn-primary" role="button" href="{{ path('app_intervention_index') }}">back to list</a>
{% endblock %}
