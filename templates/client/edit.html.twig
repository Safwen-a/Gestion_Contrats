{% extends 'base.html.twig' %}

{% block title %}Edit Client{% endblock %}

{% block body %}
  <div style=" text-align: center; margin: 0px auto; display; block; ">  <h1>Edit Client</h1> </div>

    {{ include('client/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-primary" role="button" href="{{ path('app_client_index') }}">back to list</a>

    {{ include('client/_delete_form.html.twig') }}
{% endblock %}
