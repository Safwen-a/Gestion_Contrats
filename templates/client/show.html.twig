{% extends 'base.html.twig' %}

{% block title %}Client{% endblock %}

{% block body %}
   <div style=" text-align: center; margin: 0px auto; display; block; "> <h1>Client</h1> </div>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ client.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ client.Name }}</td>
            </tr>
            <tr>
                <th>Nom_Complet</th>
                <td>{{ client.NomComplet }}</td>
            </tr>
            <tr>
                <th>Nom_charge_dossier</th>
                <td>{{ client.NomChargeDossier }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ client.description }}</td>
            </tr>
            <tr>
                <th>Pays</th>
                <td>{{ client.pays }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_client_index') }}">back to list</a>

    <a href="{{ path('app_client_edit', {'id': client.id}) }}">edit</a>

    {{ include('client/_delete_form.html.twig') }}
{% endblock %}
